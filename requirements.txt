# Core runtime — deliberately small so the agent idles at <120 MB RSS
python-telegram-bot[rate-limiter]==21.9
httpx[http2]==0.27.2
aiosqlite==0.20.0
numpy==2.1.3
python-dotenv==1.0.1
cachetools==5.5.0
